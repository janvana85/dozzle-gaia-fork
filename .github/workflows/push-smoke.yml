name: Push Smoke

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  smoke:
    name: Confirm push trigger
    runs-on: ubuntu-latest
    steps:
      - name: Print ref
        run: echo "Triggered for $GITHUB_REF at $GITHUB_SHA"
